import { PerfilSubMenuAccion } from './perfil-sub-menu-accion.model';

describe('PerfilSubMenuAccion', () => {
  it('should create an instance', () => {
    expect(new PerfilSubMenuAccion()).toBeTruthy();
  });
});
